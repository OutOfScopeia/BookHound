# Copy this file to `keystore.properties` and fill in real values.
# The real file is gitignored — do NOT commit secrets.
# Modern keytool creates a PKCS12 keystore, which uses one password
# for both the store and the key entry — put the same value in
# storePassword and keyPassword.
storeFile=../bookhound-upload.jks
storePassword=CHANGE_ME
keyAlias=upload
keyPassword=CHANGE_ME
